# ============================================
# PORTFOLIO SERVICE CONFIGURATION
# ============================================
spring.application.name=portfolio-service
server.port=8080

# ============================================
# NOTE: This service uses an in-memory PortfolioDataStore.
# No database is required for local development.
# In production (AWS), this would connect to Amazon RDS.
# ============================================

# CORS
spring.web.cors.allowed-origins=*

# Logging
logging.level.com.incedo.portfolioservice=INFO
logging.pattern.console=%d{HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
